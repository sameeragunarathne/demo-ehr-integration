[package]
org = "demo"
name = "custom_patient_service"
version = "0.1.0"
distribution = "2201.12.10"

[build-options]
optimizeDependencyCompilation = true

#[ballerina.http.traceLogAdvancedConfig]
# Enable printing trace logs on the Console. The default value is `false`.
#console = true

